using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace MyGame
{
    public enum Theme
    {
        Default,
        Rainbow,
        // Add an enum value for any new theme, like above, when adding a new theme.
        // Then add it to the dictionary at the top of GameResources
    }
}
